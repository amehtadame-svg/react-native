import { FlatList, StyleSheet, Text, View } from 'react-native'; import { FAVORITES } from '../data/mockData'; export function FavoritesScreen(): React.JSX.Element { return <View style={styles.screen}><Text style={styles.title}>Fundaciones guardadas</Text><FlatList data={FAVORITES} keyExtractor={(item) => item.id} renderItem={({ item }) => <View style={styles.card}><Text style={styles.heart}>♥</Text><View><Text style={styles.name}>{item.name}</Text><Text style={styles.muted}>{item.mission}</Text></View></View>} ListEmptyComponent={<Text style={styles.muted}>Aún no tienes guardados.</Text>} /></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#0d1117', padding: 16 }, title: { color: '#fff', fontSize: 22, fontWeight: 'bold', marginBottom: 14 }, card: { flexDirection: 'row', alignItems: 'center', gap: 12, backgroundColor: '#161b22', padding: 16, borderRadius: 12, marginBottom: 10 }, heart: { color: '#f0883e', fontSize: 22 }, name: { color: '#fff', fontWeight: '600' }, muted: { color: '#8b949e', marginTop: 5 } });
