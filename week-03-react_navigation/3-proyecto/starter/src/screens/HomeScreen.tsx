import { useNavigation } from '@react-navigation/native'; import type { NativeStackNavigationProp } from '@react-navigation/native-stack'; import { FlatList, Pressable, StyleSheet, Text, View } from 'react-native'; import { ITEMS } from '../data/mockData'; import type { HomeStackParamList } from '../navigation/types';
type Nav = NativeStackNavigationProp<HomeStackParamList, 'HomeList'>; export function HomeScreen(): React.JSX.Element { const navigation = useNavigation<Nav>(); return <View style={styles.screen}><Text style={styles.header}>Fundaciones que inspiran</Text><FlatList data={ITEMS} keyExtractor={(item) => item.id} renderItem={({ item }) => <Pressable style={styles.card} onPress={() => navigation.navigate('HomeDetail', item)}><View style={styles.info}><Text style={styles.name}>{item.name}</Text><Text style={styles.muted}>{item.mission}</Text><Text style={styles.impact}>{item.impact} · {item.city}</Text></View><Text style={styles.arrow}>›</Text></Pressable>} ItemSeparatorComponent={() => <View style={styles.sep} />} /> </View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#0d1117' }, header: { color: '#fff', fontSize: 25, fontWeight: 'bold', padding: 18 }, card: { flexDirection: 'row', alignItems: 'center', padding: 16, backgroundColor: '#161b22', marginHorizontal: 14, borderRadius: 12 }, info: { flex: 1 }, name: { color: '#fff', fontSize: 16, fontWeight: '700' }, muted: { color: '#c9d1d9', marginTop: 5 }, impact: { color: '#61DAFB', fontSize: 12, marginTop: 8 }, arrow: { color: '#61DAFB', fontSize: 28 }, sep: { height: 10 } });
