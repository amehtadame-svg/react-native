import axios from 'axios'; import type { AuthResponse, LoginCredentials, RegisterData } from '../types'; const BASE_URL = 'https://dummyjson.com'; export async function login(credentials: LoginCredentials): Promise<AuthResponse> { return (await axios.post<AuthResponse>(`${BASE_URL}/auth/login`, { ...credentials, expiresInMins: 30 })).data; } export async function register(data: RegisterData): Promise<AuthResponse> { await new Promise((resolve) => setTimeout(resolve, 700)); return { id: Date.now(), username: data.username, email: data.email, firstName: data.firstName ?? data.username, lastName: data.lastName ?? '', image: '', accessToken: `mock-access-${Date.now()}`, refreshToken: `mock-refresh-${Date.now()}` }; } export async function refreshTokens(refreshToken: string): Promise<AuthResponse> { return (await axios.post<AuthResponse>(`${BASE_URL}/auth/refresh`, { refreshToken, expiresInMins: 30 })).data; } export async function getProfile(): Promise<AuthResponse> { return (await axios.get<AuthResponse>(`${BASE_URL}/auth/me`)).data; }
