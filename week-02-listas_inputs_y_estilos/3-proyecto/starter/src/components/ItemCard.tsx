import React from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Item } from '../types';
interface Props { item: Item; onPress: (item: Item) => void; }
export function ItemCard({ item, onPress }: Props): React.JSX.Element { return <Pressable style={({ pressed }) => [styles.card, pressed && styles.pressed]} onPress={() => onPress(item)}><View style={styles.row}><Text style={styles.name}>{item.name}</Text><Text style={styles.badge}>{item.category}</Text></View><Text style={styles.mission}>{item.mission}</Text><View style={styles.meta}><Text style={styles.muted}>📍 {item.city}</Text><Text style={styles.impact}>{item.impact}</Text></View></Pressable>; }
const styles = StyleSheet.create({ card: { marginHorizontal: 16, marginVertical: 6, padding: 16, backgroundColor: '#161b22', borderWidth: 1, borderColor: '#30363d', borderRadius: 12 }, pressed: { opacity: 0.7 }, row: { flexDirection: 'row', alignItems: 'center', gap: 8 }, name: { flex: 1, color: '#fff', fontSize: 16, fontWeight: '700' }, badge: { color: '#0d1117', backgroundColor: '#61DAFB', borderRadius: 10, paddingHorizontal: 8, paddingVertical: 4, fontSize: 11 }, mission: { color: '#c9d1d9', marginTop: 8 }, meta: { flexDirection: 'row', justifyContent: 'space-between', marginTop: 12 }, muted: { color: '#8b949e', fontSize: 12 }, impact: { color: '#61DAFB', fontSize: 12, fontWeight: '600' } });
