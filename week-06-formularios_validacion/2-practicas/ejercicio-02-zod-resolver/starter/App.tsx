import React from 'react'; import { Controller, useForm } from 'react-hook-form'; import { z } from 'zod'; import { zodResolver } from '@hookform/resolvers/zod'; import { KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native'; const schema = z.object({ name: z.string().min(2, 'Escribe al menos 2 caracteres'), email: z.string().email('Correo inválido'), quantity: z.coerce.number().int('Debe ser entero').min(1, 'Mínimo 1') }); type Data = z.infer<typeof schema>; export default function App(): React.JSX.Element { const { control, handleSubmit, formState: { errors, isSubmitting } } = useForm<Data>({ resolver: zodResolver(schema), defaultValues: { name: '', email: '', quantity: 1 } }); async function submit(data: Data): Promise<void> { await new Promise((r) => setTimeout(r, 800)); console.log('Solicitud ONG válida', data); } return <KeyboardAvoidingView style={styles.root} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Solicitud de voluntariado</Text><Text style={styles.subtitle}>Validación con Zod en español</Text><Controller control={control} name="name" render={({ field }) => <Field label="Nombre" value={field.value} onChangeText={field.onChange} error={errors.name?.message} />} /><Controller control={control} name="email" render={({ field }) => <Field label="Correo electrónico" value={field.value} onChangeText={field.onChange} error={errors.email?.message} keyboardType="email-address" />} /><Controller control={control} name="quantity" render={({ field }) => <Field label="Personas voluntarias" value={String(field.value)} onChangeText={field.onChange} error={errors.quantity?.message} keyboardType="number-pad" />} /><Pressable style={styles.button} disabled={isSubmitting} onPress={handleSubmit(submit)}><Text style={styles.buttonText}>{isSubmitting ? 'Validando...' : 'Enviar solicitud'}</Text></Pressable></ScrollView></KeyboardAvoidingView>; } function Field(props: { label: string; value: string; onChangeText: (value: string) => void; error?: string; keyboardType?: 'email-address' | 'number-pad' }): React.JSX.Element { return <View><Text style={styles.label}>{props.label}</Text><TextInput style={[styles.input, props.error && styles.errorInput]} value={props.value} onChangeText={props.onChangeText} keyboardType={props.keyboardType} placeholderTextColor="#8b949e" /><Text style={styles.error}>{props.error ?? ' '}</Text></View>; } const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: '#111827' }, content: { padding: 24, gap: 8 }, title: { color: '#fff', fontSize: 23, fontWeight: 'bold' }, subtitle: { color: '#8b949e', marginBottom: 8 }, label: { color: '#c9d1d9', marginBottom: 5 }, input: { backgroundColor: '#1f2937', borderColor: '#374151', borderWidth: 1, borderRadius: 8, padding: 12, color: '#fff' }, errorInput: { borderColor: '#f85149' }, error: { color: '#f85149', minHeight: 18, fontSize: 12 }, button: { backgroundColor: '#61DAFB', padding: 14, borderRadius: 8, alignItems: 'center', marginTop: 8 }, buttonText: { color: '#111827', fontWeight: 'bold' } });
