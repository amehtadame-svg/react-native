import React from 'react'; import { Controller, useForm } from 'react-hook-form'; import { KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native'; interface Data { name: string; email: string; message: string; } export default function App(): React.JSX.Element { const { control, handleSubmit, formState: { isSubmitting } } = useForm<Data>({ defaultValues: { name: '', email: '', message: '' } }); async function submit(data: Data): Promise<void> { await new Promise((r) => setTimeout(r, 900)); console.log('Formulario ONG enviado', data); } return <KeyboardAvoidingView style={styles.root} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Contacto con fundación</Text><Text style={styles.subtitle}>React Hook Form + Controller</Text>{(['name', 'email', 'message'] as const).map((field) => <View key={field}><Text style={styles.label}>{field === 'name' ? 'Nombre' : field === 'email' ? 'Correo' : 'Mensaje'}</Text><Controller control={control} name={field} render={({ field: f }) => <TextInput style={[styles.input, field === 'message' && styles.multiline]} value={f.value} onChangeText={f.onChange} onBlur={f.onBlur} placeholder={field === 'name' ? 'Tu nombre' : field === 'email' ? 'tu@email.com' : '¿Cómo podemos ayudarte?'} placeholderTextColor="#8b949e" multiline={field === 'message'} />} /></View>)}<Pressable style={styles.button} disabled={isSubmitting} onPress={handleSubmit(submit)}><Text style={styles.buttonText}>{isSubmitting ? 'Enviando...' : 'Enviar mensaje'}</Text></Pressable></ScrollView></KeyboardAvoidingView>; } const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: '#111827' }, content: { padding: 24, gap: 12 }, title: { color: '#fff', fontSize: 23, fontWeight: 'bold' }, subtitle: { color: '#8b949e', marginBottom: 10 }, label: { color: '#c9d1d9', marginBottom: 6 }, input: { backgroundColor: '#1f2937', borderColor: '#374151', borderWidth: 1, borderRadius: 8, padding: 12, color: '#fff' }, multiline: { minHeight: 100, textAlignVertical: 'top' }, button: { backgroundColor: '#61DAFB', padding: 14, borderRadius: 8, alignItems: 'center', marginTop: 8 }, buttonText: { color: '#111827', fontWeight: 'bold' } });
