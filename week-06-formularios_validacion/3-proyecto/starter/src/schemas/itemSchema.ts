import { z } from 'zod'; export const itemSchema = z.object({ title: z.string().min(2, 'El nombre debe tener al menos 2 caracteres').max(80, 'Máximo 80 caracteres'), body: z.string().min(10, 'Describe la misión con al menos 10 caracteres').max(500, 'Máximo 500 caracteres'), category: z.string().min(2, 'Indica una categoría'), city: z.string().min(2, 'Indica una ciudad') }); export type ItemFormData = z.infer<typeof itemSchema>;
