import React from 'react'; import { Controller, type Control, type FieldPath, type FieldValues } from 'react-hook-form'; import { StyleSheet, Text, TextInput, TextInputProps, View } from 'react-native'; interface Props<T extends FieldValues> extends TextInputProps { control: Control<T>; name: FieldPath<T>; label: string; errorMessage?: string; } export function FormField<T extends FieldValues>({ control, name, label, errorMessage, ...props }: Props<T>): React.JSX.Element { return <View style={styles.container}><Text style={styles.label}>{label}</Text><Controller control={control} name={name} render={({ field }) => <TextInput {...props} style={[styles.input, errorMessage && styles.errorInput]} value={typeof field.value === 'number' ? String(field.value) : field.value ?? ''} onChangeText={field.onChange} onBlur={field.onBlur} placeholderTextColor="#8b949e" />} /><Text style={styles.error}>{errorMessage ?? ' '}</Text></View>; } const styles = StyleSheet.create({ container: { gap: 4 }, label: { color: '#c9d1d9', fontWeight: '600' }, input: { backgroundColor: '#161b22', borderColor: '#30363d', borderWidth: 1, borderRadius: 8, padding: 12, color: '#fff' }, errorInput: { borderColor: '#f85149' }, error: { color: '#f85149', minHeight: 16, fontSize: 12 } });
