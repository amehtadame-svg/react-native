import React from 'react'; import { useForm } from 'react-hook-form'; import { zodResolver } from '@hookform/resolvers/zod'; import { KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text } from 'react-native'; import { useNavigation } from '@react-navigation/native'; import { FormField } from '../components/FormField'; import { itemSchema, type ItemFormData } from '../schemas/itemSchema'; import { useCreateItem } from '../hooks/useItems';
export function CreateScreen(): React.JSX.Element { const navigation = useNavigation(); const create = useCreateItem(); const { control, handleSubmit, formState: { errors } } = useForm<ItemFormData>({ resolver: zodResolver(itemSchema), defaultValues: { title: '', body: '', category: '', city: '' } }); const submit = (data: ItemFormData): void => { create.mutate({ title: data.title, body: data.body, userId: 1 }, { onSuccess: () => navigation.goBack() }); }; return <KeyboardAvoidingView style={styles.root} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Nueva fundación</Text><FormField control={control} name="title" label="Nombre *" placeholder="Nombre de la organización" errorMessage={errors.title?.message} /><FormField control={control} name="body" label="Misión *" placeholder="Describe su misión" multiline numberOfLines={4} textAlignVertical="top" errorMessage={errors.body?.message} /><FormField control={control} name="category" label="Categoría *" placeholder="Educación, ambiente..." errorMessage={errors.category?.message} /><FormField control={control} name="city" label="Ciudad *" placeholder="Ciudad" errorMessage={errors.city?.message} /><Pressable style={styles.button} onPress={handleSubmit(submit)} disabled={create.isPending}><Text style={styles.buttonText}>{create.isPending ? 'Guardando...' : 'Crear fundación'}</Text></Pressable></ScrollView></KeyboardAvoidingView>; }
const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: '#0d1117' }, content: { padding: 20, gap: 12 }, title: { color: '#fff', fontSize: 24, fontWeight: 'bold', marginBottom: 8 }, button: { backgroundColor: '#61DAFB', padding: 14, alignItems: 'center', borderRadius: 8, marginTop: 8 }, buttonText: { color: '#0d1117', fontWeight: 'bold' } });
