import React from 'react'; import { FlatList, Pressable, StyleSheet, Text, View } from 'react-native'; import { useNavigation } from '@react-navigation/native'; import type { NativeStackNavigationProp } from '@react-navigation/native-stack'; import { ITEMS } from '../data/mockData'; import { useSavedStore } from '../stores/savedStore'; import type { HomeStackParamList } from '../navigation/types'; import type { Item } from '../types';
type Nav = NativeStackNavigationProp<HomeStackParamList, 'HomeList'>; export function HomeScreen(): React.JSX.Element { const nav = useNavigation<Nav>(); const add = useSavedStore((s) => s.addItem); const saved = useSavedStore((s) => s.items); return <View style={styles.screen}><Text style={styles.title}>Fundaciones</Text><FlatList data={ITEMS} keyExtractor={(item) => item.id} renderItem={({ item }) => <View style={styles.card}><Pressable style={styles.info} onPress={() => nav.navigate('HomeDetail', { ...item })}><Text style={styles.name}>{item.name}</Text><Text style={styles.muted}>{item.mission}</Text><Text style={styles.impact}>{item.impact} · {item.city}</Text></Pressable><Pressable style={styles.save} onPress={() => add(item)}><Text style={styles.saveText}>{saved.some((s: Item) => s.id === item.id) ? '✓' : '+'}</Text></Pressable></View>} ItemSeparatorComponent={() => <View style={styles.sep} />} /></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#0d1117' }, title: { color: '#fff', fontSize: 25, fontWeight: 'bold', padding: 18 }, card: { flexDirection: 'row', alignItems: 'center', backgroundColor: '#161b22', marginHorizontal: 14, padding: 14, borderRadius: 12 }, info: { flex: 1 }, name: { color: '#fff', fontSize: 16, fontWeight: '700' }, muted: { color: '#c9d1d9', marginTop: 5 }, impact: { color: '#61DAFB', fontSize: 12, marginTop: 6 }, save: { width: 38, height: 38, borderRadius: 19, backgroundColor: '#61DAFB', alignItems: 'center', justifyContent: 'center' }, saveText: { color: '#0d1117', fontSize: 22, fontWeight: 'bold' }, sep: { height: 10 } });
