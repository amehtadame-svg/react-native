import React from 'react'; import { FlatList, Pressable, StyleSheet, Text, View } from 'react-native'; import { useSavedStore } from '../stores/savedStore'; export function SavedScreen(): React.JSX.Element { const items = useSavedStore((s) => s.items); const remove = useSavedStore((s) => s.removeItem); const clear = useSavedStore((s) => s.clearAll); return <View style={styles.screen}><View style={styles.header}><Text style={styles.title}>Guardadas ({items.length})</Text>{items.length > 0 && <Pressable onPress={clear}><Text style={styles.clear}>Limpiar</Text></Pressable>}</View><FlatList data={items} keyExtractor={(item) => item.id} renderItem={({ item }) => <View style={styles.card}><View style={styles.info}><Text style={styles.name}>{item.name}</Text><Text style={styles.muted}>{item.mission}</Text></View><Pressable onPress={() => remove(item.id)}><Text style={styles.remove}>✕</Text></Pressable></View>} ListEmptyComponent={<Text style={styles.empty}>Guarda fundaciones desde el catálogo.</Text>} /></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#0d1117', padding: 16 }, header: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 14 }, title: { color: '#fff', fontSize: 22, fontWeight: 'bold' }, clear: { color: '#f85149' }, card: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', backgroundColor: '#161b22', borderRadius: 12, padding: 16, marginBottom: 10 }, info: { flex: 1 }, name: { color: '#fff', fontWeight: '600' }, muted: { color: '#8b949e', marginTop: 5 }, remove: { color: '#f85149', fontSize: 18 }, empty: { color: '#8b949e', textAlign: 'center', marginTop: 60 } });
