import { createBottomTabNavigator } from '@react-navigation/bottom-tabs'; import { createNativeStackNavigator } from '@react-navigation/native-stack'; import { Ionicons } from '@expo/vector-icons'; import { DetailScreen } from '../screens/DetailScreen'; import { HomeScreen } from '../screens/HomeScreen'; import { SavedScreen } from '../screens/SavedScreen'; import { useSavedStore } from '../stores/savedStore'; import type { HomeStackParamList, RootTabParamList } from './types';
const Stack = createNativeStackNavigator<HomeStackParamList>(); const Tab = createBottomTabNavigator<RootTabParamList>(); function HomeStack(): React.JSX.Element { return <Stack.Navigator screenOptions={{ headerStyle: { backgroundColor: '#161b22' }, headerTintColor: '#fff', contentStyle: { backgroundColor: '#0d1117' } }}><Stack.Screen name="HomeList" component={HomeScreen} options={{ title: 'Fundaciones' }} /><Stack.Screen name="HomeDetail" component={DetailScreen} options={({ route }) => ({ title: route.params.name })} /></Stack.Navigator>; }
export function RootNavigator(): React.JSX.Element { const count = useSavedStore((s) => s.items.length); return <Tab.Navigator screenOptions={({ route }) => ({ headerShown: false, tabBarActiveTintColor: '#61DAFB', tabBarInactiveTintColor: '#8b949e', tabBarStyle: { backgroundColor: '#161b22' }, tabBarIcon: ({ focused, color, size }) => <Ionicons name={route.name === 'Home' ? (focused ? 'home' : 'home-outline') : (focused ? 'bookmark' : 'bookmark-outline')} color={color} size={size} /> })}><Tab.Screen name="Home" component={HomeStack} options={{ tabBarLabel: 'Catálogo' }} /><Tab.Screen name="Saved" component={SavedScreen} options={{ tabBarLabel: 'Guardadas', tabBarBadge: count || undefined }} /></Tab.Navigator>; }
