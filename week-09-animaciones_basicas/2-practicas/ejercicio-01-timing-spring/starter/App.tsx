import React, { useEffect, useRef } from 'react'; import { Animated, Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native'; function Fade(): React.JSX.Element { const opacity = useRef(new Animated.Value(0)).current; useEffect(() => { Animated.timing(opacity, { toValue: 1, duration: 800, useNativeDriver: true }).start(); }, [opacity]); return <Animated.View style={[styles.card, { opacity }]}><Text style={styles.text}>Fade in con timing</Text></Animated.View>; } function Spring(): React.JSX.Element { const scale = useRef(new Animated.Value(1)).current; return <Animated.View style={{ transform: [{ scale }] }}><Pressable style={styles.button} onPressIn={() => Animated.spring(scale, { toValue: .92, useNativeDriver: true }).start()} onPressOut={() => Animated.spring(scale, { toValue: 1, tension: 300, friction: 10, useNativeDriver: true }).start()}><Text>Presióname con spring</Text></Pressable></Animated.View>; } function Parallel(): React.JSX.Element { const opacity = useRef(new Animated.Value(0)).current; const y = useRef(new Animated.Value(30)).current; useEffect(() => { Animated.parallel([Animated.timing(opacity, { toValue: 1, duration: 600, useNativeDriver: true }), Animated.timing(y, { toValue: 0, duration: 600, useNativeDriver: true })]).start(); }, [opacity, y]); return <Animated.View style={[styles.card, { opacity, transform: [{ translateY: y }] }]}><Text style={styles.text}>Parallel: fade + slide</Text></Animated.View>; } function Bounce(): React.JSX.Element { const y = useRef(new Animated.Value(0)).current; return <Animated.View style={{ transform: [{ translateY: y }] }}><Pressable style={styles.green} onPress={() => Animated.sequence([Animated.timing(y, { toValue: -20, duration: 200, useNativeDriver: true }), Animated.timing(y, { toValue: 0, duration: 200, useNativeDriver: true })]).start()}><Text>Bounce con sequence</Text></Pressable></Animated.View>; } export default function App(): React.JSX.Element { return <SafeAreaView style={styles.screen}><Text style={styles.title}>Animated API — timing y spring</Text><Fade /><Spring /><Parallel /><Bounce /></SafeAreaView>; } const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#0f172a', padding: 20, gap: 16 }, title: { color: '#61DAFB', fontSize: 21, fontWeight: 'bold', textAlign: 'center' }, card: { backgroundColor: '#1e293b', padding: 18, borderRadius: 12 }, text: { color: '#fff', fontSize: 16 }, button: { backgroundColor: '#3b82f6', padding: 15, borderRadius: 10, alignItems: 'center' }, green: { backgroundColor: '#16a34a', padding: 15, borderRadius: 10, alignItems: 'center' } });
