import React, { useRef } from 'react'; import { Animated, Pressable, StyleSheet, type ViewStyle } from 'react-native'; interface Props { children: React.ReactNode; onPress: () => void; style?: ViewStyle; } export function AnimatedCard({ children, onPress, style }: Props): React.JSX.Element { const scale = useRef(new Animated.Value(1)).current; return <Animated.View style={[styles.card, style, { transform: [{ scale }] }]}><Pressable onPress={onPress} onPressIn={() => Animated.spring(scale, { toValue: .95, useNativeDriver: true }).start()} onPressOut={() => Animated.spring(scale, { toValue: 1, tension: 300, friction: 10, useNativeDriver: true }).start()} style={styles.pressable}>{children}</Pressable></Animated.View>; } const styles = StyleSheet.create({ card: { backgroundColor: '#1e293b', borderRadius: 14, overflow: 'hidden' }, pressable: { padding: 16 } });
