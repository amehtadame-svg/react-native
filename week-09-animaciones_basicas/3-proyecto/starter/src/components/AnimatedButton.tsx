import React, { useRef } from 'react'; import { Animated, Pressable, StyleSheet, Text } from 'react-native'; interface Props { label: string; onPress: () => void; variant?: 'primary' | 'success'; } export function AnimatedButton({ label, onPress, variant = 'primary' }: Props): React.JSX.Element { const scale = useRef(new Animated.Value(1)).current; return <Animated.View style={{ transform: [{ scale }] }}><Pressable style={[styles.button, variant === 'success' && styles.success]} onPress={onPress} onPressIn={() => Animated.timing(scale, { toValue: .96, duration: 80, useNativeDriver: true }).start()} onPressOut={() => Animated.spring(scale, { toValue: 1, tension: 400, friction: 12, useNativeDriver: true }).start()}><Text style={styles.label}>{label}</Text></Pressable></Animated.View>; } const styles = StyleSheet.create({ button: { backgroundColor: '#3b82f6', borderRadius: 10, padding: 14, alignItems: 'center' }, success: { backgroundColor: '#16a34a' }, label: { color: '#fff', fontWeight: '600' } });
